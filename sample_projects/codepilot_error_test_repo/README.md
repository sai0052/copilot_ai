# CodePilot error test repo

Plain Python + pytest. There is no web framework.

The discount helper in `app/cart.py` is intentionally wrong.
`app/products.py` holds sample prices used by the cart tests.
